# docker-go

## Сборка

```
docker-compose up --build
docker-compose up -d
```

## Запуск сервера

```
docker-compose exec -it app bash

# Запустить приложение через Air (горячая перезагрузка)
air -c .air.toml
```

